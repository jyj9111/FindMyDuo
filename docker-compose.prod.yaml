version: "3.8"

services:
  redis:
    image: redis:latest
    container_name: redis-fmd
    restart: always
    ports:
      - 6379:6379
    volumes:
      - my-data:/var/lib/redis

  app:
    image: kkamacoon/fmd-prod
    container_name: fmd-prod
    restart: always
    ports:
      - 9001:9001
    depends_on:
      - redis

volumes:
  my-data: